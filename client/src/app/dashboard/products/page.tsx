"use client";

import InventoryPage from '../inventory/page';

// Reusing the Inventory Page layout for now to save time, as per plan
export default function ProductsPage() {
    return <InventoryPage />;
}
